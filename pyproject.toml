[project]
name = "easyai-lab01"
version = "0.1.0"
description = "Lab 01: Nimby experiments and report"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.0",
    "matplotlib>=3.8",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.uv]
package = false